# Development

## Dependencies

Run the following to install all dependencies:

```bash
$ npm install
```

## Build

Two steps are necessary to build the project entirely:

```bash
$ npm run build
```